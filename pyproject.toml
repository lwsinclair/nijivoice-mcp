[project]
name = "nijivoice-mcp"
version = "0.2.0"
description = "Model Context Protocol (MCP) implementation for NijiVoice API with FastMCP v2"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
dependencies = [
    "fastmcp>=2.1.0",
    "mcp[cli]>=1.6.0",
    "pydantic>=2.11.3",
    "httpx>=0.24.0",
    "python-dotenv>=1.0.0",
    "rich>=13.9.4",
    "pytest>=8.3.5",
    "pytest-asyncio>=0.26.0",
]
